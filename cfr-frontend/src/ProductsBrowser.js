import React from 'react';
import SearchBar from './SearchBar';

const ProductsBrowser = (props) => {
    return (
        <div>
             <SearchBar></SearchBar>
             <h1> ProductsBrowser here!</h1>
        </div>

    );
}

export default ProductsBrowser;
